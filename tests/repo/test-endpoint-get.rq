#+ summary: Sample query for testing SPARQL endpoint method
#+ endpoint: "http://test-endpoint/transform/sparql/"
#+ transform: {
#+     "key": "?p",
#+     "value": "?o",
#+     "$anchor": "key"
#+   }
#+ endpoint-method: GET

select ?p ?o where {
  ?_id_iri ?p ?o
} LIMIT 5
